select#playlists
    each playlist, index in user.playlists
        option(value=playlist.uri, selected=index === 0 ? true : false)=playlist.name
label Playlists
button.btn.waves-effect.waves-light.indigo.darken-1(data-target="playlist-modal") New Playlist
#widget